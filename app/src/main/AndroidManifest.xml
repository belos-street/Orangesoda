<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orangesoda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/org1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/org1"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddActivity" />
        <activity android:name=".Ui.AboutUsActivity" />
        <activity android:name=".Ui.MainDrawerLayout" />
        <activity android:name=".Ui.MainListActivity" />
        <activity android:name=".Ui.SettingActivity" />
        <activity android:name=".Ui.TextEditActivity"
            >

        </activity>
    </application>

</manifest>